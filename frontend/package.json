{
  "name": "williw-frontend",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "description": "Williw Tauri Webview UI — TypeScript",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.5",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
